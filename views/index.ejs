<%-include('partials/header.ejs')%>
<body>
    <%-include('partials/navbar.ejs')  %>  
         <!--botão de test
            <a class="btn btn-primary" href="/pergunta/31">Testar links</a>
        -->

        <div class="container">
            <hr>
            <h1 class="agaum">Perguntas:</h1>
            <hr>
            <a href="/perguntar" class="btn btn-info" >Perguntar</a> <a href="/principal" class="btn btn-primary" >Página Principal</a> 
            <br>
            <% perguntas.forEach(pergunta =>{ %>
                <br>
                <div class="card">
                    <div class="card-body">
                        <h3 >Título: <%= pergunta.titulo%></h3>
                    </div>
                    <div class="card">
                        <div class="card-body">
                            <h3>Descrição: <%= pergunta.textarea%></h3>
                        </div>
                     <div class="card-footer">
                    <a href="pergunta/<%= pergunta.id %>"> <!--buscou cada página por id e inseriu alert no canto inferior da página sobre cada id -->
                        <button  class="btn btn-primary">Responder</button></a>
                    </div> 
                </div>
            <% }) %>
        </div>
        <%-include('partials/footer.ejs')  %> 
</body>